<tr>
  <td><%= post.id %></td>
  <td><%= post.date %></td>
  <td><%= truncate(post.rationale, length: 30) %></td>
  <td><%= post.user.full_name %></td>
  <td><%= link_to "Edit", edit_post_path(post), id: "edit_#{post.id}" %></td>
</tr>